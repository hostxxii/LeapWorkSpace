# 2026-02-19_DOM废弃路线说明

日期：2026-02-19（历史路线归档说明整理于 2026-02-23）  
来源拆分：根 `SOLUTION.md` 的 2026-02-19 时间线条目（DOM Bridge / DOM Final Shape / DOM Full / `dom树.md`）

## 定位（maintenance 中的历史记录，非现行手册）

- 本文档仅用于历史回溯与术语对照。
- 不作为当前实现方案、开发指引或维护手册入口。
- 现行实现与验证应以 `leap_manual/architecture/`、`leap_manual/operations/`、`tests/` 为准。

## 迁移校验状态（补录）

- 状态：已过期（仅历史）
- 整理日期：2026-02-23
- 来源路径：根 `SOLUTION.md` 的 2026-02-19 时间线条目（原文件已收口；历史快照见 `leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`）
- 当前适用性：仅用于历史回溯与术语对照，不可直接作为当前实现依据
- 纠偏规则：涉及现行 DOM / DoD 主线时，以 `leap_manual/architecture/*`、`tests/*` 与当前源码为准

## 准确性说明（来源约束）

- 本记录基于根 `SOLUTION.md` 当前时间线状态整理，重点是“废弃结论”和“使用边界”。
- 原阶段性过程文档/执行 TODO 在当前仓库中未统一保留为完整链路，因此不重建细节步骤。
- 本文档避免复述旧路线实现细节，防止对现行 DoD / 当前 DOM 路线造成误导。

## 已归档的历史路线（统一说明）

以下条目均为历史废弃路线，不应作为当前实现依据：

1. DOM Bridge 重设计 Phase 1-4 完整闭环（Binary Tree Spec）
2. DOM Final Shape 顺序执行（`DOM_FINAL_SHAPE_EXEC_TODO`）
3. DOM Full 主线收口（P1-P8 全量闭环）
4. `dom树.md` 复核与文档同步（对应历史路线文档）

## 废弃原因（摘要）

- DOM 路线后续发生主线切换，历史方案不再代表当前工程结构。
- `native` / `yoga` 相关旧路径结论已不适合作为现行维护依据（根 `SOLUTION.md` 已明确标注）。
- 继续在手册主线保留旧路线细节会混淆当前开发与回归判断。

## 当前应参考的内容（替代入口）

- 架构与现行设计：
  - `leap_manual/architecture/DoD布局引擎.md`
  - `leap_manual/architecture/并发池（线程池与进程池）.md`
  - `leap_manual/architecture/Window与全局对象模型.md`
- 回归与压测入口：
  - `tests/runners/run-full.ps1`
  - `tests/runners/run-perf.ps1`
  - `tests/manifest/full.ps1`
  - `tests/manifest/perf.ps1`
- 维护记录索引（现行维修过程）：
  - `leap_manual/maintenance/INDEX.md`

## 使用建议

- 需要解释历史术语（如 Binary Tree Spec / DOM Final Shape）时，可引用本文档。
- 需要编写或修改当前代码、脚本、手册时，不要以本文档作为行为依据。
- 若未来需要补充旧路线细节，应继续放入 `maintenance/` 并标注“历史/已过期”，不要回流到现行手册主线。

## 相关入口

- 维护索引（含历史/废弃记录）：`leap_manual/maintenance/INDEX.md`
- 时间线快照（收口前，仅历史回溯）：`leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`

