# 2026-02-20_DoD迁移Phase2C收口与验收摘要

日期：2026-02-20（迁移摘要整理于 2026-02-23）  
来源拆分：根 `SOLUTION.md`（“DoD 迁移 Phase 2C 完成 + 最终验收”）

## 迁移校验状态（补录）

- 状态：已校验（迁移摘要/维护记录）
- 整理日期：2026-02-23
- 来源路径：根 `SOLUTION.md` 对应时间线条目（原文件已收口；历史快照见 `leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`）
- 当前适用性：可作为现行维护/回溯入口，但涉及实现细节时应与当前源码、`tests/results/*`、`leap_manual/*` 交叉核对
- 纠偏规则：若与历史时间线原表述冲突，以当前源码、脚本实测结果和手册 SSOT 文档为准

## 准确性说明（来源约束）

- 本记录基于当前仓库中的 DoD 架构文档、验证脚本与近期回归结果整理。
- 关键依据：
  - `leap_manual/architecture/DoD布局引擎.md`
  - `tests/scripts/integration/test-leapenv-dod-*.js`
  - `tests/scripts/integration/test-leapenv-threadpool-dod.js`
  - `tests/scripts/perf/bench-dom-multi-scale.js`
  - `tests/scripts/perf/bench-dom-final-shape-baseline.js`
  - `tests/results/20260223_091736_full/summary.txt`
  - `tests/results/20260223_091618_perf/perf-summary.md`
- 因此本文是“收口与验收映射摘要”，不重建 Phase 2C 的逐步实现日志。

## 背景

`SOLUTION.md` 中的“DoD 迁移 Phase 2C 完成 + 最终验收”可拆分为两个维度：

- 实现收口：DoD 布局引擎与 DOM/线程池集成能力形成可维护主线
- 验收闭环：`full` / `perf` 中存在可重复执行的测试与压测入口

后续仓库重组后，这些能力的“事实来源”已经主要转移到 `tests/scripts/*`、`tests/results/*` 和 `leap_manual/architecture/*`。

## 实现收口（当前可见形态）

### 1. DoD 核心能力（JS 侧）

核心模块提供：

- `DoDTree`（TypedArray 连续存储）
- `DomToDoDConverter`（DOM → DoD 转换）
- `DoDLayoutEngine`（布局计算）
- `DoDLayoutBenchmark`（基准辅助）

相关实现：

- `leap-env/src/impl/dod-layout-engine.js`

### 2. DoD 功能验证（集成脚本）

当前 root `full` 档位已纳入的 DoD 相关脚本（物理迁移后路径）：

- `tests/scripts/integration/test-leapenv-dod-layout.js`
- `tests/scripts/integration/test-leapenv-dod-integration.js`
- `tests/scripts/integration/test-leapenv-dod-converter.js`
- `tests/scripts/integration/test-leapenv-threadpool-dod.js`

这些脚本覆盖了：

- DoD 布局基础计算与对比
- Mock DOM → DoD 转换
- 与线程池协同运行（并发与任务形态）

### 3. DoD 性能验收（压测脚本）

当前 root `perf` 档位中与 DoD 验收强相关的脚本：

- `tests/scripts/perf/bench-dom-multi-scale.js`
- `tests/scripts/perf/bench-dom-final-shape-baseline.js`

作用摘要：

- 多规模对比（不同节点规模下的趋势观察）
- final-shape 对比门禁（吞吐 / p99 / RSS 增长 / failureRate / timeoutRate）

## 验收状态（当前快照）

### 1. `full`（功能完整回归）

参考：

- `tests/results/20260223_091736_full/summary.txt`

与 DoD 相关项当前表现（快照）：

- `leapenv-dod-layout`：PASS
- `leapenv-dod-integration`：PASS
- `leapenv-threadpool-dod`：PASS
- `leapenv-dod-converter`：FAIL（当前已知基线失败项之一）

说明：

- `leapenv-dod-converter` 当前处于已知失败基线，不应在此文档中误写为“全项通过”。
- 这不否定 Phase 2C 的主线收口结论，但提示“验收闭环”中仍有专项用例待后续跟进。

### 2. `perf`（性能门禁/趋势观察）

参考：

- `tests/results/20260223_091618_perf/perf-summary.md`

当前快照（修复 `perf` 收尾稳定性后的样本）：

- `bench-dom-multi-scale`：PASS
- `bench-dom-final-shape-baseline`：PASS

这说明 DoD 相关压测入口仍可用，并能输出持续对比数据。

## 与现行文档体系的关系

- 设计与原理：`leap_manual/architecture/DoD布局引擎.md`
- 执行方法与结果位置：`leap_manual/operations/压测执行手册.md`
- 基线与阈值：`tests/baselines/perf-baseline.md`、`tests/baselines/thresholds.md`

## 相关入口

- `SOLUTION.md`
- `leap_manual/maintenance/INDEX.md`
- `tests/runners/run-full.ps1`
- `tests/runners/run-perf.ps1`





