# 2026-02-23_根SOLUTION退场前引用去依赖清单（准备）

日期：2026-02-23  
来源：严格版清单 `REPO_REORG_TODO_STRICT_SSOT.md`（`F3/G4` 最终收口前准备）

## 迁移校验状态（补录）

- 状态：已校验（准备清单 / 执行前盘点）
- 整理日期：2026-02-23
- 来源路径：仓库内 `*.md` 对 `SOLUTION.md` 的扫描结果（本地扫描）
- 当前适用性：用于最终删除根 `SOLUTION.md` 前的引用去依赖准备；不代表立即执行删除
- 纠偏规则：最终执行删除前需再次全量扫描，以最新仓库状态为准

## 背景

严格版目标最终希望移除仓库中所有 `SOLUTION.md`。当前子模块 `SOLUTION.md` 已删除，仅剩根 `SOLUTION.md`（兼容跳转页）。

但根 `SOLUTION.md` 仍承担“兼容入口”角色，且仓库内存在多类引用：

- 入口导航引用（未来需要改掉）
- 历史来源/历史记录引用（可保留或改为快照）
- 计划/TODO 历史记录中的文本提及（通常无需改动）

因此在最终 README 统一重写之前，先做一份引用分类清单，避免到删除时混淆“该改”和“不该改”的引用。

## 事实校验（补录）

- 核对依据：
  - `rg -n "SOLUTION\\.md" --glob "*.md"`（仓库全量 Markdown 扫描）
  - `README.md`
  - `leap_manual/README.md`
  - `leap_manual/maintenance/INDEX.md`
  - `leap_manual/maintenance/INDEX.md`
- 核对结论：
  - 当前仓库仅存在一个物理文件 `SOLUTION.md`（根目录）
  - 子模块 `leap-env/SOLUTION.md`、`leap-vm/SOLUTION.md` 已退场；剩余提及大多为历史来源说明
- 纠偏说明：
  - 扫描结果中的大量 `SOLUTION.md` 命中属于 `REPO_REORG_*` 执行记录与 `maintenance` 历史来源描述，并不等于“阻塞删除”的入口依赖

## 删除前引用分类（执行时按此处理）

### A. 必须改为 `maintenance` 直达入口（删除阻塞项）

这些是“入口导航型引用”，删除根 `SOLUTION.md` 前应改掉：

- `README.md`（根目录导航）
- `leap_manual/README.md`（手册总入口中的相关入口）
- `leap_manual/maintenance/INDEX.md`（当前仍列出根 `SOLUTION.md` 兼容入口）
- `leap-env/README.md` / `leap-vm/README.md`（仍提及根 `../SOLUTION.md` 的兼容入口说明）
- `leap_manual/reference/LEAPVM_API.md` 中“相关记录入口”若直接写根 `SOLUTION.md`
- `leap_manual/maintenance/2026-02-19_DOM废弃路线说明.md` 的“时间线索引：`SOLUTION.md`”条目（应改到 `maintenance` 中的时间线快照或维护索引）

建议替换目标：

- 现行维修入口：`leap_manual/maintenance/INDEX.md`
- 历史回溯入口：`leap_manual/maintenance/INDEX.md`
- 收口前时间线快照：`leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`

预处理进度（2026-02-23，非最终 README 阶段）：

- 已处理：`leap_manual/reference/LEAPVM_API.md` 中 Intl 相关记录入口已改为直达 `leap_manual/maintenance/2026-02-22_Intl_ICU修复.md`
- 已处理：`leap_manual/maintenance/2026-02-19_DOM废弃路线说明.md` 的“时间线索引”已改为 `leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`
- 已处理：`leap_manual/maintenance/INDEX.md` 已移除根 `SOLUTION.md` 兼容入口条目，并补充现行维修入口指向 `leap_manual/maintenance/INDEX.md`
- 待最终 README 阶段处理：`README.md`、`leap_manual/README.md`、`leap-env/README.md`、`leap-vm/README.md`

### B. 可保留的历史来源描述（非删除阻塞）

这些引用通常位于维护记录（含历史记录）中，用于说明“来源拆分自根 `SOLUTION.md`”：

- `leap_manual/maintenance/*.md` 的 `来源拆分`、`来源路径`
- `leap_manual/maintenance/*.md` 中带“已过期（仅历史）”标记的历史来源说明
- `leap_manual/architecture/*.md` 中“来源拆分：根 README + `SOLUTION.md`”的来源标注

处理原则：

- 可以保留（作为历史来源说明）
- 若用户希望“仓库中不再出现 `SOLUTION.md` 文本”，则统一改写为：
  - “来源拆分：根时间线快照（见 `leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`）”

### C. 计划 / TODO / 执行记录中的历史提及（通常不改）

以下文件属于过程文档，保留历史提及更有价值：

- `REPO_REORG_PLAN.md`
- `REPO_REORG_TODO.md`
- `REPO_REORG_TODO_STRICT_SSOT.md`

处理原则：

- 默认不改（作为改造过程证据）
- 仅在最终验收对“全仓文本去 `SOLUTION.md`”有硬要求时，才额外做替换策略

## 最终删除执行顺序（建议）

1. 完成 `G4`：统一重写各层 README（根/内层）并收敛入口
2. 按本清单处理 A 类入口导航引用
3. 再次执行 `rg -n "SOLUTION\\.md" --glob "*.md"`，确认仅剩 B/C 类历史提及（或按目标继续清理）
4. 删除根 `SOLUTION.md`
5. 再跑一次全量扫描与 `smoke`

## 相关入口

- `REPO_REORG_TODO_STRICT_SSOT.md`（`F3` / `G4`）
- `leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`
- `leap_manual/maintenance/2026-02-23_SOLUTION时间线快照_收口前.md`
- `leap_manual/maintenance/INDEX.md`

